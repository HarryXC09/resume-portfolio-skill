---
name: resume-portfolio-skill
description: 从用户简历、人物动作视频和机构 Logo 复刻交互式个人简历网站。适用于下载本项目后收集素材、分析履历、提炼双语文案并替换模板内容的任务。
---

# 简历 Portfolio 复刻

以本 Skill 所在目录为项目根目录，保留 React、GSAP 滚动动画、卡片布局和响应式视觉。用户请求制作个人网站时，先检查素材，再根据真实简历完成整个网站。这个仓库包含可运行源码，不需要特定 Agent、云服务或 API Key。

## 首次接手

1. 阅读 `references/materials.md`，运行 `node scripts/intake.mjs`。脚本创建固定素材目录并输出缺项。
2. 用用户的语言一次性说明：简历放 `inputs/resume/`；动作视频放 `inputs/videos/`；照片放 `inputs/photos/`；学校、工作单位 Logo 放 `inputs/logos/`；可选模型放 `inputs/models/`。给出材料指南中的镜头建议，提醒将绿幕布光均匀且人物服装避免绿色。
3. 检查已提交材料，缺简历时等待，不能虚构履历。缺部分视频时先分析简历并完成文案；告诉用户哪些区块暂用照片/重复镜头，哪些素材仍需补齐。空目录及占位图不算用户材料。
4. 读取 `references/adaptation.md`，执行事实提取、文案映射、素材加工、组件替换和浏览器验收。用户说材料已准备好后重新运行 intake，不重复询问是否继续。

## 事实与内容

- 简历原文是人物事实源。将每项拟发布事实与原文页码/段落记入 `work/facts.md`；区分正式职称、学习阶段、参与和主导、已发表和投稿中。冲突或日期不明确时只询问影响输出的缺项。
- `src/content/profile.js` 是页面文案与媒体编号的唯一数据入口。不要同时维护另一份脱节的 profile.json。常规复刻优先改这份数据；结构不适配时可调整组件，不硬凑六个能力或四个经历。
- 生成简洁中文文案及忠实英文翻译；保留人物的实际专业方向。通用的医学/显微镜/肿瘤视觉不得被解释为新人资历。非医学人物应替换 Research 的标题、图注、模型或改为项目图片。
- 对外联系方式由用户提供并指定公开；未指定的电话、生日、住址、证件等不发布。原始简历和原视频只留在 inputs，不能复制进 public。

## 素材与交付

使用 `npm run media` 处理编号 01–07 的绿幕 MP4，参数与镜头映射见 materials。处理后检查边缘、肤色和循环接缝；脚本参数是起点，不是对任意绿幕都适用的质量保证。

依次运行 `npm ci`、`npm run build`、`npm run preview`。通过可用的浏览器工具检查桌面与手机、双语、轮播、滚动、透明视频和无模型降级。用一个确实不同的新人 Profile 验证替换，搜索所有姓名/机构/联系方式和占位文案，不能仅凭 build 成功宣称复刻完成。

交付本地预览、完成的网站源码、素材用途索引及验收结果。默认不上传用户新素材、不自动部署。发布或上传时遵循用户当前授权。缺材料导致的占位区块明确列出。
