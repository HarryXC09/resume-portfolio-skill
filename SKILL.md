---
name: resume-portfolio-skill
description: 从用户简历、人物动作视频和机构 Logo 复刻交互式个人简历网站。适用于下载本项目后收集素材、分析履历、提炼双语文案并替换模板内容的任务。
---

# 简历 Portfolio 复刻

以本 Skill 所在目录为项目根目录，保留 React、GSAP 滚动动画、卡片布局和响应式视觉。用户请求制作个人网站时，先检查素材，再根据真实简历完成整个网站。这个仓库包含可运行源码，不需要特定 Agent、云服务或 API Key。

## 首次接手

1. 阅读 `references/materials.md`，运行 `node scripts/intake.mjs`。脚本创建固定素材目录并输出缺项。
2. 用用户的语言一次性说明：简历放 `inputs/resume/`；动作视频放 `inputs/videos/`；照片放 `inputs/photos/`；学校、工作单位 Logo 放 `inputs/logos/`；可选模型放 `inputs/models/`。给出材料指南中的镜头建议，提醒将绿幕布光均匀且人物服装避免绿色。
3. 检查已提交材料，缺简历时等待，不能虚构履历。缺部分视频时先分析简历并完成文案；告诉用户哪些区块暂用照片/重复镜头，哪些素材仍需补齐。空目录及占位图不算用户材料。
4. 读取 `references/adaptation.md`，执行事实提取、文案映射、素材加工、组件替换和浏览器验收。用户说材料已准备好后重新运行 intake，不重复询问是否继续。
5. 使用 `template/resume-portfolio-content-template-general.docx` 作为文字输入表。它沿用维护者优化版的四列表格排版；先填写身份、About、教育/工作经历、联系方式，再高密度填写 `modules[]`。Word 表中的 `modules[]` 不预设行业，Agent 要根据 CV 选择项目经历、个人作品集、奖项荣誉、研究、论文、客户案例或专业实践等类型；初稿可填 3 个模块，首个模块最多 4 个条目，人工确认后再删减。仓库以 `template/resume-portfolio-layout-source.docx` 作为去除个人元数据的版式来源；需要重建通用字段版本时运行 `python scripts/adapt_user_word_template.py`，不要写回来源文件。

## 事实与内容

- 简历原文是人物事实源。将每项拟发布事实与原文页码/段落记入 `work/facts.md`；区分正式职称、学习阶段、参与和主导、已发表和投稿中。冲突或日期不明确时只询问影响输出的缺项。
- `src/content/profile.js` 是页面文案与媒体编号的唯一数据入口。Word 表是用户编辑入口，`profile.json` 只是转换中间产物，不能与页面长期并行维护。常规复刻优先改这份数据；结构不适配时可调整组件，不硬凑六个能力或四个经历。
- 生成简洁中文文案及忠实英文翻译；保留人物的实际专业方向。Clinical、Research、Publication 只是原 Demo 的视觉槽位，不是行业限制。非学术人物应把它们映射为项目经历、个人作品集、奖项荣誉、客户案例或专业实践，并同步修改标题、图注和模型说明；通用的医学/显微镜/肿瘤视觉不得被解释为新人资历。
- 对外联系方式由用户提供并指定公开；未指定的电话、生日、住址、证件等不发布。原始简历和原视频只留在 inputs，不能复制进 public。

## 素材与交付

使用 `npm run media` 处理编号 01–07 的绿幕 MP4，参数与镜头映射见 materials。处理后检查边缘、肤色和循环接缝；脚本参数是起点，不是对任意绿幕都适用的质量保证。

依次运行 `npm ci`、`npm run intake`、Word 转换 / 内容映射、`npm run media`、`npm run build`、`npm run preview`。通过可用的浏览器工具检查桌面与手机、双语、轮播、滚动、透明视频和无模型降级。先把预览部署到静态托管或本地可访问地址，用户点击验收并提出修改；修改优先回到 Word 表，再同步到 profile 和网页。用户明确最终确认后，才执行正式部署。用一个确实不同的新人 Profile 验证替换，搜索所有姓名/机构/联系方式和占位文案，不能仅凭 build 成功宣称复刻完成。

## 标准化闭环与卡点

下载项目后先收集 `inputs/` 素材，再读取简历事实并填写 Word 表；素材处理和文案映射完成后构建静态站，先给用户一个可预览地址；用户的修改意见回写 Word 表并重新构建，最后在明确确认后部署。静态站默认不依赖后端服务器，但视频、字体和 3D 模型仍会影响包体积、首屏速度和托管额度。

常见卡点包括：简历格式复杂导致事实提取不完整、绿幕边缘溢色、Logo 没有公开授权、视频或 GLB 过大、部分 Agent 没有 DOCX 解析库、静态托管无法处理大文件或 SPA 回退。解决方式是保留页码/段落事实索引、先用照片降级、记录素材来源、压缩 WebM/GLB、使用 `scripts/word_to_profile.py` 和 bundled Python、把大素材放对象存储或改用静态 CDN，并在每轮构建后做真实浏览器验收。

交付本地预览、完成的网站源码、素材用途索引及验收结果。默认不上传用户新素材、不自动部署。发布或上传时遵循用户当前授权。缺材料导致的占位区块明确列出。
